pipeline {
  agent any
  stages {
    stage('compile') {
      steps {
        bat 'hostname'
      }
    }
    stage('test') {
      steps {
        bat 'ipconfig'
      }
    }
  }
}
am fine oka but how
